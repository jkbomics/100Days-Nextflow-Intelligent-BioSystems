Day 31 — Automated HTML report generation.

Implemented an end-to-end reporting system that aggregates pipeline outputs into a single HTML report.
Improved usability and interpretability by centralizing QC, alignment, differential expression, and enrichment results.

This step transforms the pipeline into a user-friendly and publication-ready workflow.